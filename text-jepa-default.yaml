tokenizer:
  model_name: Qwen/Qwen3-0.6B
  max_length: 512
  mask_token: "[MASK]"

masking:
  mask_ratio: 0.15
  max_block_words: 2

model:
  hidden_dim: 768
  num_heads: 12
  num_layers: 6
  ffn_dim: 3072
  dropout: 0.0
  norm: rms
